name: Publish to npm

on:
  # Release = a version tag pushed from a maintainer's machine
  # (npm version patch -> git push --follow-tags). PR merges to
  # main do NOT publish — only the tag push does — so external
  # contributions land via PR without triggering a release, and
  # the local check-version/check-tags hooks stay the gate.
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # OIDC — trusted publishing, no stored token

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: true

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      # No npm pin needed: Node 24 bundles npm 11.x, which is exactly the
      # range we want — >= 11.5.1 for trusted publishing, and short of
      # npm 12. Do not move to npm 12 yet: it blocks non-registry fetches
      # by default (EALLOWREMOTE), which breaks a from-scratch lockfile
      # regen via transitive remote tarballs. Its 12.0.0 sigstore crash
      # looks fixed in 12.0.1, but the fetch block is the live blocker.
      - name: Install dependencies
        run: npm ci

      - name: Check
        run: npm run check

      - name: Build
        run: npm run build

      - name: Publish
        run: npm publish
